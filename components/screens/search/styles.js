import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 10,
  },
  tabContainer: {
    minHeight: 64,
  },
  tab: {
    fontSize: 20,
    fontFamily: 'open-sans-reg'
  }
});